name: "staging-11026 ubuntu-24.04-arm"
on:
  push:
    branches: ["pr-11026-xplat-ci"]
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
defaults:
  run:
    shell: bash
jobs:
  test:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30
    env:
      UNSLOTH_COMPILE_DISABLE: '1'
      UNSLOTH_IS_PRESENT: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r studio/backend/requirements/studio.txt pytest pytest-asyncio pytest-timeout python-multipart
      - run: python -m pip install transformers
      - run: rc=0; PYTHONPATH=studio/backend python -m pytest studio/backend/tests/multi_account/test_chat_load_status.py studio/backend/tests/multi_account/test_chat_shared_resident.py studio/backend/tests/multi_account/test_chat_unload_ownership.py studio/backend/tests/multi_account/test_media_unload_handoff.py studio/backend/tests/test_account_inference_isolation.py studio/backend/tests/test_account_media_isolation.py studio/backend/tests/test_cache_inventory_path_boundary.py studio/backend/tests/test_cached_gguf_routes.py studio/backend/tests/test_diffusion_routes.py studio/backend/tests/test_export_absolute_paths.py studio/backend/tests/test_hub_token_caller_identity.py studio/backend/tests/test_offline_local_cache_discovery.py studio/backend/tests/test_saved_image_metadata_precision_contract.py studio/backend/tests/test_video_routes.py -q --tb=short -k 'not llama_cpp_load_progress_live and not TestGpuAutoSelection and not TestPreSpawnGpuResolution and not TestPerGpuFitGuardAllCounts and not TestTransformersIntrospection and not test_returns_cuda_when_cuda_available and not test_calls_cuda_cache_when_cuda' > pytest_out.txt 2>&1 || rc=$?; cat pytest_out.txt; if [ "$rc" = "5" ]; then echo "no tests ran (deps absent on this runner)"; rc=0; fi; if [ "$rc" = "2" ] && grep -qE "No module named .(torch|unsloth_zoo|transformers)." pytest_out.txt && ! grep -qE "^(FAILED|ERROR) " pytest_out.txt; then echo "collection needs a dep this runner does not ship"; rc=0; fi; if [ "$rc" = "1" ] && ! grep -qE "^ERROR " pytest_out.txt; then tot=$(grep -cE "^FAILED " pytest_out.txt); dep=$(grep -cE "^FAILED .* No module named .(torch|unsloth_zoo|transformers).$" pytest_out.txt); if [ "$tot" -gt 0 ] && [ "$tot" = "$dep" ]; then echo "only tests needing a dep this runner does not ship failed"; rc=0; fi; fi; exit "$rc"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Node tests
        working-directory: studio/frontend
        run: node --experimental-strip-types --test tests/chat-cache-miss-download-label.test.ts
