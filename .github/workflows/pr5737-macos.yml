name: PR #5737 macos-14
on:
  push:
    branches: [pr5737-cross-os]
    paths:
      - 'unsloth_cli/**'
      - 'studio/backend/core/inference/llama_server_args.py'
      - 'studio/backend/tests/test_llama_server_args.py'
      - 'studio/backend/run.py'
      - '.github/workflows/pr5737-macos.yml'
  pull_request:
    branches: [main]
    paths:
      - 'unsloth_cli/**'
      - 'studio/backend/core/inference/llama_server_args.py'
      - 'studio/backend/tests/test_llama_server_args.py'
      - 'studio/backend/run.py'
      - '.github/workflows/pr5737-macos.yml'
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  pytest:
    runs-on: macos-14
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12', cache: 'pip' }
      - name: Install minimal test deps (no torch, no MLX)
        run: |
          python -m pip install --upgrade pip
          python -m pip install typer pytest pytest-asyncio pytest-timeout pyyaml pydantic
      - name: Show env
        run: |
          python --version
          pip list | head -30
          uname -a
          sw_vers
      - name: CLI tests
        run: |
          python -m pytest unsloth_cli/tests/ -q -v
      - name: Backend boundary-validator tests
        run: |
          PYTHONPATH=studio/backend python -m pytest studio/backend/tests/test_llama_server_args.py -q -v
