name: "staging-8766 ubuntu-latest"
on:
  push:
    branches: ["pr-8766-xplat-ci"]
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
defaults:
  run:
    shell: bash
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      UNSLOTH_COMPILE_DISABLE: '1'
      UNSLOTH_IS_PRESENT: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r studio/backend/requirements/studio.txt pytest pytest-asyncio pytest-timeout python-multipart
      - run: python -m pip install --index-url https://download.pytorch.org/whl/cpu 'torch>=2.4,<2.11' || echo "optional deps unavailable on ubuntu-latest"
      - run: rc=0; PYTHONPATH=studio/backend python -m pytest studio/backend/tests/test_diffusion_backend.py studio/backend/tests/test_diffusion_engine_load_contract.py studio/backend/tests/test_diffusion_gated_base.py studio/backend/tests/test_diffusion_krea2.py studio/backend/tests/test_diffusion_offline_load.py studio/backend/tests/test_diffusion_prequant.py studio/backend/tests/test_diffusion_sdxl.py studio/backend/tests/test_media_auto_switch.py studio/backend/tests/test_media_family_assembler_offline.py studio/backend/tests/test_media_keepwarm.py studio/backend/tests/test_media_locality_cache_layout.py studio/backend/tests/test_openai_auto_download.py studio/backend/tests/test_openai_auto_switch.py studio/backend/tests/test_video_backend.py studio/backend/tests/test_video_offline_load.py -q --tb=short -k 'not llama_cpp_load_progress_live and not TestGpuAutoSelection and not TestPreSpawnGpuResolution and not TestPerGpuFitGuardAllCounts and not TestTransformersIntrospection and not test_returns_cuda_when_cuda_available and not test_calls_cuda_cache_when_cuda' > pytest_out.txt 2>&1 || rc=$?; cat pytest_out.txt; if [ "$rc" = "5" ]; then echo "no tests ran (deps absent on this runner)"; rc=0; fi; if [ "$rc" = "2" ] && ! grep -qE "^FAILED " pytest_out.txt; then eall=$(awk '/^E   /{n++} END{print n+0}' pytest_out.txt); edep=$(awk '/^E   ModuleNotFoundError.* No module named .(torch|unsloth_zoo|transformers).$/{n++} END{print n+0}' pytest_out.txt); if [ "$eall" -gt 0 ] && [ "$eall" = "$edep" ]; then echo "collection needs a dep this runner does not ship"; rc=0; fi; fi; if [ "$rc" = "1" ] && ! grep -qE "^ERROR " pytest_out.txt; then tot=$(grep -cE "^FAILED " pytest_out.txt); dep=$(grep -cE "^FAILED .* No module named .(torch|unsloth_zoo|transformers).$" pytest_out.txt); if [ "$tot" -gt 0 ] && [ "$tot" = "$dep" ]; then echo "only tests needing a dep this runner does not ship failed"; rc=0; fi; fi; exit "$rc"
